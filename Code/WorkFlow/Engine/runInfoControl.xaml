<UserControl x:Class="Engine.runInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Content="刷新" Height="23" HorizontalAlignment="Left"  Name="refreshButton" VerticalAlignment="Stretch"  Width="75" Click="refreshButton_Click" />
        <Button Content="清空" Height="23" HorizontalAlignment="Left"  Name="clearButton" VerticalAlignment="Stretch"  Width="75" Margin="90,0,0,0" Click="clearButton_Click" />
        <DataGrid AutoGenerateColumns="True"  Name="runInfoDataGrid"  Grid.Row="1" />
   
    </Grid>
</UserControl>
